class Solution {
    public void reverseString(char[] s) {
     int n = s.length;
     char temp;
     int end = n - 1;
    for (int i = 0; i < end; i++) {
        temp = s[i];
        s[i] = s[end];
        s[end] = temp;
        end--;
        }
    }
}
